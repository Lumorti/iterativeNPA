// Standard includes
#include <iostream>
#include <vector>

// Import MOSEK
#include "fusion.h"

// Import Eigen
#include <Eigen/Dense>

// Define the type for the polynomials
typedef std::vector<int> monomial;
typedef std::vector<std::pair<double, monomial>> polynomial;

// Generic entry function
int main(int argc, char* argv[]) {

    // Settings
    int numX = 3;
    int numY = 3;
    int numA = 2;
    int numB = 2;

    // Define the Bell functional

    // 

    // Exit without errors
    return 0;

}
